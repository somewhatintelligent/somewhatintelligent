{
  "name": "better-auth-manifest",
  "version": "0.0.0",
  "private": true,
  "description": "What a Better Auth deployment has switched on, derived from the configuration it runs.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vp test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "better-auth": "^1.6.25",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "better-auth": "^1.6.25"
  }
}

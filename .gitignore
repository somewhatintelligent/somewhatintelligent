# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# dotenv environment variable files
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/

# Alchemy local state, bundles and logs
.alchemy

# vite+ installs git hooks here and points core.hooksPath at it. `vp config`
# runs from `prepare`, so a fresh clone regenerates it on install.
.vite-hooks

# TanStack Start / router generated dev artefacts
.tanstack
.wrangler
.output

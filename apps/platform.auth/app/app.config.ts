/**
 * The brand strings, in one place so a fork rebrands by editing this file
 * rather than hunting literals through the components.
 */
export const appConfig = {
  brand: {
    /** Full product name. Shown in wordmarks, page titles, and OG images. */
    name: "somewhatintelligent",
    /** Short uppercase wordmark variant (stacked logo layout). */
    short: "SI",
    /** Support address for outbound transactional email. */
    supportEmail: "hello@somewhatintelligent.ca",
  },
} as const;

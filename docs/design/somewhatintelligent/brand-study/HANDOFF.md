# Historical somewhatintelligent implementation handoff

> **Status: stale implementation record.**

This handoff belonged to the previous somewhatintelligent repository before the current platform site was built. Its branch name, package paths, worker topology, implementation sequence, baseline failures, and verification commands are not instructions for this repository.

The current implementation already translates the mockup direction into working software. Use these locations as the operative source:

- `apps/platform.site` — public site, shop, product, writing, software, About, cart, checkout, and orders.
- `packages/platform.design` — brand tokens, typography, radii, fonts, generated CSS, and mark implementation.
- `packages/platform.ui` — shared semantic UI components.
- `apps/platform.commerce` — catalog, availability, pricing, checkout, orders, reservations, media, and settlement.
- `apps/platform.auth` — identity and account surfaces.

The enduring art direction remains in [`01-brand-theory.md`](./01-brand-theory.md) and [`02-interface-and-voice.md`](./02-interface-and-voice.md). The mockups and reference images remain visual source material for new surfaces and future refinement.
